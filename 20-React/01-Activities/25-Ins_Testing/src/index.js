import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//we do .render, attach it to the highest level component, attach it to the root element of the html page
ReactDOM.render(<App />, document.getElementById('root'));
