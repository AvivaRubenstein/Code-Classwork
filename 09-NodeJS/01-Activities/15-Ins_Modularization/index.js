const badmath = require('./badmath.js');
//we can create and require our own libraries, and bring it in w relative paths to something in our file system

console.log(badmath.pie);

console.log(badmath.predictable());
