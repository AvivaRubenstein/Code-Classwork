const badmath = require('badmath');

console.log(badmath.pie);

console.log(badmath.predictable());

//badmath is a package that exists, we are going to use node package manager to get it
//type npm init -y     to initialize and accept all defaults ---this will automatically fill a package.json file
//npm install badmath (or i which is short for install)